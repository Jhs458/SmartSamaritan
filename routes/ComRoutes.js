var express = require('express');
var router = express.Router();
var mongoose = require('mongoose');
var Commodities = mongoose.model('Commodities');


var passport = require('passport');

module.exports = router;
